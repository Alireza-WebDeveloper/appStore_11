import { ExitIcon } from '@/app/lib/design/common/icons';
import LinkContainer from '@/app/lib/design/common/link-container';

const SignOut = () => {
  return (
    <div className="mt-auto border-t border-white/10 pt-4">
      {/* <LinkContainer
        href={'/auth/sign-out'}
        className="w-full flex items-center justify-center gap-2 text-sm text-red-400 hover:text-red-300 transition-colors"
      >
        <ExitIcon color="white" />
        خروج
      </LinkContainer> */}
    </div>
  );
};

export default SignOut;
